apiVersion: v1
kind: ServiceAccount
metadata:
  name: external-secrets
  namespace: infra
  annotations:
    # Replace with the IAM role ARN that grants Secrets Manager read for External Secrets Operator
    eks.amazonaws.com/role-arn: arn:aws:iam::000000000000:role/aistartup-external-secrets
