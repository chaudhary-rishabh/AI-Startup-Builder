apiVersion: v1
kind: Service
metadata:
  name: analytics-service
  namespace: user-services
  labels:
    app.kubernetes.io/name: analytics-service
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: analytics-service
  ports:
    - name: http
      port: 8080
      targetPort: http
