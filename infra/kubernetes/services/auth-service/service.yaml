apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: user-services
  labels:
    app.kubernetes.io/name: auth-service
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: auth-service
  ports:
    - name: http
      port: 8080
      targetPort: http
