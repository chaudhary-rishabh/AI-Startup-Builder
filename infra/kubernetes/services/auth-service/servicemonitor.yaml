apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: auth-service
  namespace: user-services
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: auth-service
  namespaceSelector:
    matchNames:
      - user-services
  endpoints:
    - port: http
      path: /metrics
      interval: 30s
