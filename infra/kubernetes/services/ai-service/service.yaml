apiVersion: v1
kind: Service
metadata:
  name: ai-service
  namespace: user-services
  labels:
    app.kubernetes.io/name: ai-service
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: ai-service
  ports:
    - name: http
      port: 8080
      targetPort: http
