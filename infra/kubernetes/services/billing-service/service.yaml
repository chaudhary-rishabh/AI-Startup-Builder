apiVersion: v1
kind: Service
metadata:
  name: billing-service
  namespace: user-services
  labels:
    app.kubernetes.io/name: billing-service
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: billing-service
  ports:
    - name: http
      port: 8080
      targetPort: http
