apiVersion: v1
kind: Service
metadata:
  name: project-service
  namespace: user-services
  labels:
    app.kubernetes.io/name: project-service
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: project-service
  ports:
    - name: http
      port: 8080
      targetPort: http
