apiVersion: v1
kind: Service
metadata:
  name: rag-service
  namespace: user-services
  labels:
    app.kubernetes.io/name: rag-service
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: rag-service
  ports:
    - name: http
      port: 8080
      targetPort: http
