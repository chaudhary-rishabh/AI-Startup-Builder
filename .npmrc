# Hoist Storybook packages to the root node_modules so Vite can resolve
# inter-package imports (e.g. storybook/internal/preview/runtime, @storybook/react)
# without them needing to be direct deps of every consumer.
public-hoist-pattern[]=@storybook/*
public-hoist-pattern[]=storybook
